{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Backend (module)",
            "type": "python",
            "request": "launch",
            "module": "backend.main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}